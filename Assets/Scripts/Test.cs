using UnityEngine;

// im making this script to test if github is working for me
// if this exists on your end, dm in the programming channel "bongo" and @ me
// then try deleteing this if you can

public class Test : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
